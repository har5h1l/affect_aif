\section{Methods}

\subsection{Trust game}

A focal active-inference agent plays a repeated trust game \cite{Berg1995Trust,KingCasas2005Trust} with four partners over an episode of up to 200 rounds. Each partner has a latent \emph{type} (cooperator, reciprocator, exploiter, or random) and a latent \emph{disposition} toward the agent (trusting, neutral, or hostile)---neither directly observable; both must be inferred from behavioral evidence.

In the binary game both players choose to cooperate or defect. Table~\ref{tab:payoff} gives the focal agent's payoffs. The structure creates a genuine inference problem: defecting is individually tempting regardless of partner behavior, but a cooperative partner is worth engaging, so the agent must infer the partner's type and disposition to choose well. In the graded regime, the binary choice is replaced by a discretized investment level; the agent commits a resource along a continuous scale rather than making a binary commitment. This distributes policy probability mass across several options, giving partner-local affective precision meaningful room to influence which level is selected. A partner-choice regime adds a prior decision each round: the agent selects which partner to engage before choosing an action, turning approach and avoidance into explicit policy outputs.

\begin{table}[t]
\centering
\caption{Focal agent payoffs in the binary trust game.}
\label{tab:payoff}
\begin{tabular}{lcc}
\toprule
 & Partner cooperates & Partner defects \\
\midrule
Agent cooperates & $3$ & $-1$ \\
Agent defects & $5$ & $1$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Per-partner generative models}

The focal agent maintains a separate discrete generative model $\mathcal{M}_k$ for each partner $k$, rather than a single pooled social model. This keeps evidence about each partner's behavior independent and---critical for the analysis below---allows the confidence with which each model is acted upon to differ across partners. For partner $k$:
\begin{align}
s_k &= (s_k^{\text{type}},\; s_k^{\text{disp}}) & &\text{(hidden: partner type and disposition)}\label{eq:states}\\
o_k &= (o_k^{\text{act}},\; o_k^{\text{pay}}) & &\text{(observed: partner response and payoff)}\label{eq:obs}\\
a   &\in \mathcal{A}                            & &\text{(cooperate/defect or investment level)}\label{eq:act}
\end{align}
The model $\mathcal{M}_k = \{A^{\text{act}}_k,\, A^{\text{pay}}_k,\, B_k,\, C_k,\, D_k,\, E_k\}$ encodes likelihoods ($A^{\text{act}}_k$, $A^{\text{pay}}_k$), transition dynamics ($B_k$), prior beliefs ($D_k$), preferred observations ($C_k$), and policy priors ($E_k$). Payoff enters as an observation modality rather than an external reward cache, so the agent's preferences are part of the same inference problem as its partner beliefs. State and policy inference use the official \texttt{pymdp} discrete active-inference implementation \cite{Heins2022Pymdp,DaCosta2020DiscreteSynthesis}.

\subsection{Partner-local affective precision}

Hesp et al.\ \cite{Hesp2021DeeplyFeltAffect} treat the policy precision parameter $\gamma$ as an inferred quantity rather than a fixed hyperparameter. The agent maintains a higher-order belief $\beta$ over its expected action precision, representing subjective model fitness---how well the generative model is supporting confident action. An affective charge signal $\phi$ drives updates to $\beta$: it rises when the model performs better than a prior baseline and falls when it performs worse, making it a derivative of model fitness that responds to changes in predictive performance rather than to absolute reward level.

We extend this to the per-partner case. After interacting with partner $k$, the agent computes surprisal from the predicted probability of the observed partner response:
\begin{equation}
\epsilon_k =
-\log P\!\left(o_k^{\text{act}} = \hat{o}_k^{\text{act}} \;\middle|\; q(s_k^{\text{type}}, s_k^{\text{disp}})\right).
\label{eq:epsilon}
\end{equation}
This is computed from partner \emph{action} rather than payoff because action is the most direct test of type-by-disposition model fit: a reliably defecting partner produces no action surprise even though the payoff is poor. We set the neutral fitness baseline at $\sigma_0^2 = (-\log 0.5)^2$, so a fifty-fifty binary prediction carries zero affective charge.

Affective charge is:
\begin{equation}
\phi_k = \alpha\!\left(\sigma_0^2 - \epsilon_k^2\right),
\label{eq:charge}
\end{equation}
where $\sigma_0^2$ is a prior fitness baseline and $\alpha$ is a gain parameter. Positive $\phi_k$ means partner $k$'s model predicted better than the baseline; negative $\phi_k$ means it predicted worse. A categorical posterior $q(\beta_k)$ over discrete levels $\{0.5, 0.67, 1.0, 1.5, 2.0\}$ is updated each round by $\phi_k$. Higher $\beta_k$ represents lower expected model fitness---the agent has accumulated evidence that its model of this partner is unreliable. The partner-specific policy precision is:
\begin{equation}
\gamma_k = \gamma_{\text{base}} \,/\, \mathbb{E}_{q(\beta_k)}[\beta_k].
\label{eq:gamma}
\end{equation}
Note the direction: $\beta_k$ represents precision \emph{volatility}, not precision itself. Higher $\beta_k$ means the model has been less reliable, producing a broader, less committed policy distribution. This runs opposite to the intuition that higher $\beta$ equals more confidence --- it is the inverse that drives action sharpening. A well-validated partner model drives $\mathbb{E}[\beta_k]$ toward low values, raising $\gamma_k$ and sharpening policy selection for that partner. An unreliable partner model keeps $\mathbb{E}[\beta_k]$ high, lowering $\gamma_k$ and leaving policy selection diffuse. The mechanism thus translates accumulated evidence about predictive reliability into action confidence---independently for each social relationship.

The $\beta_k$ posterior is maintained as an external auxiliary tracker rather than a proper hidden state in the generative model, which simplifies implementation at the cost of losing the agent's ability to form prior expectations over its own future affective states (discussed in Section~\ref{sec:discussion}).

\begin{figure}[t]
\centering
\small
\setlength{\fboxsep}{5pt}
\begin{tabular}{c@{\hspace{0.7em}}c@{\hspace{0.7em}}c}
\fbox{\begin{minipage}{0.27\linewidth}\centering
Per-partner beliefs\\
$q_k(s_k^{\text{type}}, s_k^{\text{disp}})$
\end{minipage}}
&
$\rightarrow$
&
\fbox{\begin{minipage}{0.27\linewidth}\centering
Prediction error\\
$\epsilon_k \xrightarrow{\phi_k} q(\beta_k)$
\end{minipage}}
\\[0.8em]
& $\searrow$ & $\downarrow$ \\
&
\fbox{\begin{minipage}{0.27\linewidth}\centering
Action confidence\\
$\gamma_k = \gamma_{\text{base}} / \mathbb{E}[\beta_k]$
\end{minipage}}
& $\rightarrow$ \quad action toward $k$
\end{tabular}
\caption{Partner-local affective precision. Each partner has an independent belief state and precision estimate. Prediction error on partner action updates $\beta_k$, which sets how sharply the agent commits to policies involving that partner.}
\label{fig:model}
\end{figure}

\paragraph{Tracked-only condition.}
To establish that any behavioral effect of the signal runs specifically through action confidence---rather than through indirect effects on belief quality---we include a tracked-only variant. This variant retains the $\beta_k$ update in full but fixes $\gamma_k = \gamma_{\text{base}}$ for all partners at all times, severing the link from the precision estimate to the softmax temperature. The $\beta_k$ posterior evolves normally and is available for analysis, but it never changes how policies are selected. If tracked-only behavior matches the no-affect baseline while full precision modulation differs from both, the behavioral effect runs through the $\gamma_k$ pathway. If tracked-only matches full precision modulation, the effect is mediated by belief quality, not action sharpening.

\paragraph{Simulation scale.}
Diagnostic sweeps use small seed counts to identify mechanisms and failure modes; they are not treated as the final evidence standard for central behavioural claims. The phenotype experiments use 20 seeds per condition over 200-round episodes, which is the planned scale for descriptive individual-difference profiles and manuscript figures. Central behavioural confirmations use 30 seeds per condition. The abrupt-betrayal confirmation uses a 120-round episode because the switch is scheduled explicitly and the primary readouts are post-switch reallocation, return latency, conditional payoff, entropy, and joint accuracy. Open-regime, lesion, social-allocation, and mixed-volatility profiles use 200 rounds so that learning curves, partner allocation, and beta trajectories can stabilise over multiple interaction phases.
