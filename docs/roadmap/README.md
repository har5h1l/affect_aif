# Roadmap

This directory is for forward plans, milestone sequencing, and revisit queues.

Keep immediate run status, blockers, and verification gates in `docs/active/`.
Keep one-off agent transfer packets in `docs/handoffs/`. Existing roadmap
content may still live in `docs/future/roadmap.md`; move durable roadmap
content here only when a task explicitly calls for that migration.
