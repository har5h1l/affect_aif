# Decisions

This directory records stable architecture and experiment decisions plus their
revisit conditions.

Use `docs/active/` for current operating state, `docs/handoffs/` for
task-specific transfer packets, and `docs/results/` for interpreted evidence.
When an experiment assumption changes, update both the relevant decision note
and the experiment or theory documentation that depends on it.
