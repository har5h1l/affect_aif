# Reference

This directory is for stable commands, schemas, contracts, and background notes
that are reusable across tasks.

Do not use reference notes for current run status or result interpretation.
Put live state in `docs/active/`, interpreted evidence in `docs/results/`, and
task-specific transfer packets in `docs/handoffs/`.
