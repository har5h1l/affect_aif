# Experiment Decisions

## Hypothesis Spine

The current experiment surface follows the Hesp-extension H1-H7 hypotheses in
`docs/theory/hypotheses.md`.

## Evidence Status

Current evidence requires a completed run on the current apashea-aligned,
factorized-control architecture, with provenance recorded under `docs/results/`.
Historical or partial outputs are not comparable unless rerun or explicitly
marked as historical.

## Full Runs

Full experiments should wait until the restructure and verification gate pass.
The immediate post-verification queue is recorded in
`docs/state/current/next_runs.md`.

## External Benchmarks

CvC and CoGames are future external benchmark directions. They are separate from
the trust-task evaluation arena and should not block trust-task hypothesis work.
