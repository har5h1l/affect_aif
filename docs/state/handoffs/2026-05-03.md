# Handoff: 2026-05-03

## Summary

The old conductor state was replaced by `docs/state/` as the current steering
surface. The active mission is now the reusable AIF core and task-package reset
around the Hesp-extension H1-H7 hypothesis spine.

## Salvaged From Conductor

- Prior detached H5 and clinical-betrayal reruns stopped without final
  `results.csv` files.
- The clinical phenotypes rerun was absent in the recorded worktree state.
- Prior shallow-confirm and factorial outputs are not comparable to the current
  architecture after factorized controls and environment decoding changed.
- Result interpretation remains gated on user approval and fresh current-run
  provenance.

## Next Agent Should

1. Use `docs/state/current/mission.md` as the active mission.
2. Check `docs/state/current/blockers.md` before interpreting any results.
3. Run the verification gate in `docs/state/current/next_runs.md` before any
   full experiment rerun.

## Restructure Verification

- Branch: `master`
- Verified commit: `54dc0eac9e0f85b69d8813852644be707b28bfec`
- Local verification commands:
  - `python -m pytest tests/ -q` -> `357 passed, 13 skipped, 2 warnings`
  - `python -m ruff check .` -> pass
  - `python -m mypy` -> pass
  - `git diff --check` -> pass
- Verification was run once on the restructure branch and again after
  fast-forwarding `master`.

## Mango Sync

- Timestamp: `2026-05-03 05:31 UTC` (`2026-05-02 22:31 PDT`)
- Target: `server:~/repos/affect_aif`
- Sync command: Mango `sync-push.sh` with
  `PROJECT_LOCAL_PATH=/Users/harshilshah/Desktop/Active Inference/affect_aif/.worktrees/codex-reusable-aif-task-packages`
  because the registered project path still had unrelated local deletions.
- Equivalent project flow: `mango cloud sync push affect_aif`
- Result: sync succeeded.
- Remote cleanup: removed stale remote-only repo surfaces left behind by
  non-deleting rsync (`agent/`, `env/`, `experiment/`, `archive/`,
  `conductor/`, `docs/paper/`, old moved configs, old moved benchmark/CvC
  files, old demo notebooks, and removed tests/scripts).
- Remote follow-up: if future remote work needs git-clean status rather than a
  synced workspace, push `master` to origin and reset the server checkout from
  origin before launching long-running jobs.
