# Notebooks

These notebooks are exploratory companions to the supported package code.

They are useful for manual inspection and demos, but the supported reproducible workflow is the CLI plus saved results under `results/`.
