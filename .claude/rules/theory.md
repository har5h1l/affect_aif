---
paths:
  - "affect_aif/core/**"
  - "affect_aif/generative_model/**"
  - "affect_aif/agent/**"
  - "docs/theory.md"
---

# Theory Rules

- Use "per-partner metacognitive precision tracking" (never "interoceptive social inference")
- Use "augmentation" (never "compensation" or "depth replacement")
- Beta update rule is variationally grounded via Hesp et al. — never call it a "heuristic"
- Policy selection minimizes expected free energy — never frame as reward maximization
- Active inference loop order: perception -> learning -> policy selection -> action
- When changing core math, verify against equations in docs/theory.md
- Use the theory-checker agent for review when modifying these files
